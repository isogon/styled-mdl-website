import React from 'react'
import { Button } from 'styled-mdl'

const demo = () => <Button accent>Accent</Button>
const caption = 'Accent Colored Button'
const code = '<Button accent>Accent</Button>'

export default { demo, caption, code }
