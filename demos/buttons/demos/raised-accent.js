import React from 'react'
import { Button } from 'styled-mdl'

const demo = () => (
  <Button raised accent>
    Action
  </Button>
)
const caption = 'Accent Colored'
const code = `/*----- Accent colored raised button -----*/
<Button raised>Action</Button>`

export default { demo, caption, code }
