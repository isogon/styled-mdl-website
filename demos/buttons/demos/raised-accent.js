import React from 'react'
import { Button } from 'styled-mdl'

const demo = () => (
  <Button raised accent>
    Raised Accent
  </Button>
)
const caption = 'Accent Colored Raised Button'
const code = '<Button raised accent>Raised Accent</Button>'

export default { demo, caption, code }
