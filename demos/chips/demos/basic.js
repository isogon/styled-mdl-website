import React from 'react'
import { Chip } from 'styled-mdl'

const demo = () => <Chip>Basic Chip</Chip>
const caption = 'Basic Chip'
const code = '<Chip>Basic Chip</Chip>'

export default { demo, caption, code }
