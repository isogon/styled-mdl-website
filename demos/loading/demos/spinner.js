import React from 'react'
import { Spinner } from 'styled-mdl'

const demo = () => <Spinner active />

const caption = 'Spinner'
const code = '<Spinner active />'

export default { demo, caption, code }
