import React from 'react'
import { Slider } from 'styled-mdl'

const demo = () => <Slider min={0} max={100} />

const caption = 'Default Slider'
const code = '<Slider min={0} max={100} />'

export default { demo, caption, code }
