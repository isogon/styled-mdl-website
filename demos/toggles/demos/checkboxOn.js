import React from 'react'
import { Checkbox } from 'styled-mdl'

const demo = () => <Checkbox label="Checkbox" defaultChecked />

const caption = 'Checkbox On'
const code = '<Checkbox label="Checkbox" defaultChecked />'

export default { demo, caption, code }
